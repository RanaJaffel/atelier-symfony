<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des Auteurs</title>
    <!-- Ajout de Bootstrap via CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5">
    <h2 class="mb-4 text-center text-primary">📚 Liste des Auteurs</h2>

    <div class="mb-3 text-end">
        <a href="{{ path('app_add') }}" class="btn btn-success">➕ Ajouter des auteurs</a>
    </div>

    <table class="table table-bordered table-striped table-hover shadow-sm bg-white">
        <thead class="table-dark text-center">
            <tr>
                <th>ID</th>
                <th>Username</th>
                <th>Email</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for author in authors %}
                <tr class="align-middle text-center">
                    <td>{{ author.id }}</td>
                    <td>{{ author.username }}</td>
                    <td>{{ author.email }}</td>
                    <td>
                        <a href="{{ path('app_update', {'id': author.id}) }}" class="btn btn-warning btn-sm">
                            ✏️ Modifier
                        </a>
                        <a href="{{ path('author_delete', {'id': author.id}) }}" 
                           onclick="return confirm('Voulez-vous vraiment supprimer cet auteur ?');"
                           class="btn btn-danger btn-sm">
                            🗑️ Supprimer
                        </a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4" class="text-center text-muted">Aucun auteur trouvé</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>

</body>
</html>
